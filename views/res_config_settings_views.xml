<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.inventory.expense</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="inside">
                <app data-string="Inventory Expense" string="Inventory Expense" name="inventory_expense">
                    <block title="OpenAI Configuration">
                        <setting string="OpenAI Model" help="The model to use for receipt extraction (e.g., gpt-4o-mini, gpt-4o)">
                            <field name="openai_model"/>
                        </setting>
                    </block>
                </app>
            </xpath>
        </field>
    </record>
</odoo>
